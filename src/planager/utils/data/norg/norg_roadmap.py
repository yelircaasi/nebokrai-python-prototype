from pathlib import Path
from typing import Dict, List

#from planager import entities
from planager.utils.data.norg.norg_utils import norg_utils as norg


def read_roadmap(filepath: Path) -> Dict:
    roadmap = ...
    return roadmap


def read_roadmaps_list_from_workspace(workspace_root: Path) -> List:
    roadmap = ...
    return roadmap


def write_roadmap(roadmap, filepath: Path) -> None:
    ...