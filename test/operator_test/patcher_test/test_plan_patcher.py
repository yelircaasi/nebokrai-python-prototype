import pytest

from planager.operator.patcher.plan_patcher import PlanPatcher


class PlanPatcherTest:
    def test_init(self) -> None:
        assert True

    def test_call(self) -> None:
        assert True
