import pytest

from planager.operator.patcher.schedule_patcher import SchedulePatcher


class SchedulePatcherTest:
    def test_init(self) -> None:
        assert True

    def test_call(self) -> None:
        assert True
